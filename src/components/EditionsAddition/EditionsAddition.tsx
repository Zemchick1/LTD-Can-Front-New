import styles from "./EditionsAddition.module.sass";

export const EditionsAddition = () => {
    return (
        <form className={styles.editionForm}>
            <button>Add new edition</button>
        </form>
    )
}